% Non-Linear Optimization Lab 4
% Newton's Method
clc;
clear;
close all;

xn = 0:0.1:1;
% 使用匿名函数描述函数，嵌套描述被微分的函数
syms x;
f = inline(x.^2,'x');
df = inline(diff(f(x)),'x');
x0 = 5;

% 迭代初始化
t=0;
xi = x0;
while (df(xi) >=0.00001)
    xn_new=xi-f(xi)./(df(xi));
    xi=xn_new;
    t=t+1;
end

opt_x=xi;
opt_y=f(opt_x);
title({'Optimal point is ',num2s

