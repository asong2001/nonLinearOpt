% 不用function制作
v1=transpose([-2 2]);
v2=transpose([2 1]);
v3=[v1 v2];

e1=v1/norm(v1);
t2=v2-(transpose(v2)*e1)*e1;
e2=t2/norm(t2);

figure(1);
plot(v1);
grid on;
hold on
plot(e1);

plot(v2);
grid on;
hold on
plot(e2);

axis(-2,2,-1,2);